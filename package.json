{
  "name": "one-pager",
  "version": "1.0.0",
  "description": "AI-assisted workflow helper for generating crisp and high-quality one-pager documents",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/bordenet/one-pager.git"
  },
  "scripts": {
    "test": "NODE_OPTIONS='--experimental-vm-modules' jest",
    "test:unit": "NODE_OPTIONS='--experimental-vm-modules' jest --testPathPattern=\\.test\\.js$",
    "test:e2e": "playwright test",
    "test:coverage": "NODE_OPTIONS='--experimental-vm-modules' jest --coverage",
    "test:watch": "NODE_OPTIONS='--experimental-vm-modules' jest --watch",
    "lint": "eslint js/**/*.js",
    "lint:fix": "eslint js/**/*.js --fix",
    "format": "eslint js/**/*.js --fix",
    "quality": "npm run lint && npm run test:coverage",
    "prepare": "command -v pre-commit >/dev/null 2>&1 && pre-commit install || echo 'pre-commit not installed, skipping hook setup'"
  },
  "keywords": [
    "ai",
    "workflow",
    "one-pager",
    "claude",
    "gemini",
    "document-generation"
  ],
  "author": "Matt J Bordenet",
  "license": "MIT",
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@playwright/test": "^1.40.0",
    "eslint": "^8.55.0",
    "fake-indexeddb": "^6.2.5",
    "idb": "^8.0.3",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
